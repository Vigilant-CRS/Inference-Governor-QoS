//! Die aus `proto/oip/grpc_service.proto` erzeugten Typen.
//!
//! Der Inhalt ist generiert und wird nicht von Hand bearbeitet. Die
//! Workspace-Lints gelten hier nicht: sie beschreiben, wie *wir* Code
//! schreiben, nicht wie `prost` ihn erzeugt.

#![allow(
    clippy::pedantic,
    clippy::arithmetic_side_effects,
    clippy::indexing_slicing,
    clippy::panic,
    clippy::unwrap_used,
    missing_docs,
    unreachable_pub
)]

tonic::include_proto!("inference");
