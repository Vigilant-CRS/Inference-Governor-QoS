[package]
name = "onetimer-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "OneTimer scheduling core: pure, deterministic, dependency-free"

# Bewusst ohne Dependencies.
# Spec 8.4: Scheduling-Core ohne Triton-Abhaengigkeit testbar.
# Spec 30.1: deterministischer Core, I/O strikt ausserhalb.
# Null Dependencies macht ausserdem `cargo deny` fuer den Kern trivial.
[dependencies]

[lints]
workspace = true
