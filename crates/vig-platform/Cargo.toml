[package]
name = "vig-platform"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license = "BUSL-1.1"
description = "Lesende Hardwarebeobachtung fuer den Vigilant Inference Governor"
repository = "https://github.com/Vigilant-CRS/Inference-Governor-QoS"
publish = false

[dependencies]
# Fuer Aufzeichnung und Replay von Snapshots. Sonst nichts: dieses Crate
# liest Text und Dateien, es spricht mit keiner Bibliothek des Herstellers.
serde = { version = "1.0.229", features = ["derive"] }
serde_norway = "0.9.42"

[lints]
workspace = true
